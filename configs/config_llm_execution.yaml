llm_provider: "openai" # Options: openai, llama3, anthropic, cohere, mistral, t5
mode: "light" # Options: full, light

prompt: "v4" # Prompt to use from prompts.yaml
data_path: "data/processed/ground_truth_synthetic.xlsx" # Path to the data file (YAML, CSV or XLSX)

store_results: true
results_dir: "runs"
test_name: "v4_GroundTruthSynthetic_openaiGPT4oMini" # convention: <version_of_prompt>_<data>_<model_name>
